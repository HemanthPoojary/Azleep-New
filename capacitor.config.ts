import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.azleep.urbanwhispers',
  appName: 'Azleep Urban Whispers',
  webDir: 'dist'
};

export default config;
